<%= turbo_frame_tag "article-form" do %>
  <div class="article-form">
    <%= render 'shared/errors', object: @article %>
    <%= form_with model: @article do |f| %>
      <div class="columns">
	<div class="column is-three-quarters">
	  <div class="field">
	    <%= f.label "Название", class: "label" %>
	    <div class="control">
	      <%= f.text_field :title, class: "input" %>
	    </div>
	  </div>
	  <div class="field">
	    <%= f.label "Текст", class: "label" %>
	    <div class="control">
	      <%= f.text_area :body, class: "input" %>
	    </div>
	  </div>
	  <div>
	    <%= f.collection_select :tag_ids, tags, :id, :title, {}, multiple: true,
	     data: {controller: 'select'} %>
	  </div>
	</div>
	<div class="column">
	  <%= f.label "Изображения (необязательно):", class: "file-label mb-2" %>
	  <%= render "pictures_field" %>
	</div>
      </div>
      <div class="control">
	<%= f.submit (@article.new_record? ? 'Опубликовать' : "Изменть"), class: 'button is-link' %>
      </div>
    <% end %>
  </div>
<% end %>
